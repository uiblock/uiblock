import { faCircleExclamation } from '@fortawesome/free-solid-svg-icons'
import { Icon, IconProps } from './icon'

export const ErrorIcon = (props: IconProps) => <Icon {...props} icon={faCircleExclamation} />
