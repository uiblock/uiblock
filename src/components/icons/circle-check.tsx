import { faCircleCheck } from '@fortawesome/free-solid-svg-icons'
import { Icon, IconProps } from './icon'

export const CircleCheckIcon = (props: IconProps) => <Icon icon={faCircleCheck} {...props} />
